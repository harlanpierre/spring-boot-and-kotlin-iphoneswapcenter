cors:
  originPatterns: http://localhost:3000, http://localhost:8080
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/iphone_swap_center?useTimezone=true?serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
springdoc:
  paths-to-match:
    - /**
  swagger-ui:
    use-root-path: true
