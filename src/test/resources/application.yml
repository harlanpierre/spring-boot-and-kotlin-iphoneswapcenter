server:
  port: 8888
cors:
  originPatterns: http://localhost:3000,http://localhost:8080
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
springdoc:
  paths-to-match:
    - /**
  swagger-ui:
    use-root-path: true
